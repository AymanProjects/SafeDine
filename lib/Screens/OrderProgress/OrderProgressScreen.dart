import 'package:flutter/material.dart';

class OrderProgressScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {

    return Scaffold(
      body: Center(child: Text('Order progress Screen'),),
    );
  }
}